server:
  port: 8761  # Eureka Server 的服务端口
eureka:
  client:
    register-with-eureka: false  #是否将当前的Eureka Server 服务作为客户端 进行注册
    fetch-registry: false  #是否获取其他的Eureka Server  服务数据
    service-url:
      defaultZone: http://localhost:8761/eureka/ #注册中心的访问地址   端口后面的名字一定不可以用这个项目名

